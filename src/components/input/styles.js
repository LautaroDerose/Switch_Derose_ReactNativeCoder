import { StyleSheet } from "react-native";
import colors from "../../constants/colors";

export const styles = StyleSheet.create({
    container: {
        borderBottomColor: colors.gameOnTeriaryContainer,
        borderBottomWidth: 1,
        paddingVertical: 10,
        textAlign: 'center',
    }
});